services:
  musicdl:
    image: ghcr.io/fightroad/musicdl:latest
    ports:
      - "8000:8000"
    environment:
      HOST: 0.0.0.0
      PORT: 8000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
